{
  "name": "saltfish-library",
  "private": true,
  "scripts": {
    "dev": "npm --prefix apps/api run start:dev & npm --prefix apps/web run dev",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "db:migrate": "npm --prefix apps/api run db:migrate",
    "db:generate": "npm --prefix apps/api run db:generate",
    "start": "npm --prefix apps/api run start:prod"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.7",
    "@types/node": "^20.0.0",
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0"
  }
}
